{{header}}

<h1>{{menu.index}}</h1>

<h2 class="announce">Welcome to the demo !</h2>

<p>Tsux is a library designed to made C++ web application (it's means the application stay alive between two requests) in a simple way.</p>

<p>Tsux is based on FastCGI (especially libfcgi and libfcgi++) and try to be simple as possible.</p>

<h3>Features:</h3>

<ul>
<li>   usage of C++ stream to write HTTP response </li>
<li>   easy access to usefull data </li>
<li class="down"><ul>
  <li>       regular post/get data</li>
  <li>       post file data (multipart/form-data enctype parsing)</li>
  <li>       webserver params</li>
  <li>       cookies</li>
  <li>       response header</li>
</ul></li>
<li>   simple routing system with POSIX extended regex</li>
<li>   simple module system</li>
<li>   simple template system</li>
<li>   mimetypes matching</li>
<li>   regex, dirent encapsulations and easy file loading</li>
<li>   base encoder/decoder</li>
<li>   a translator system</li>
<li>   a session system</li>
</ul>

Tsux don't have the goal to implement current web technologies, but to let you the capacity to build simple website in C++.
{{footer}}
